преобразование текста в нижний регистр и замена всех символов кроме букв на пробелы

/*std::ifstream ifs{ "../resources/bfile003.txt" };
    if (!ifs) std::cout << "Error!";

    std::ofstream ofs{"../resources/file003.txt"};

    char tmp;
    while (ifs.get(tmp))
    {
        tmp = std::tolower(tmp);
        if (tmp < 'a' || tmp > 'z') tmp = ' ';
        ofs.put(tmp);
    }*/

